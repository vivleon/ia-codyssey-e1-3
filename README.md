# Mini NPU Simulator

Python의 기초 문법으로 MAC 연산을 직접 만들어 보는 프로젝트입니다.

클래스나 외부 라이브러리를 사용하지 않았습니다. 초보자가 흐름을 따라갈 수
있도록 **리스트, 딕셔너리, 반복문, 조건문, 작은 함수**로 구성했습니다.

## 1. 실행 방법

Python 3.8 이상이 필요합니다.

```bash
python3 main.py
```

실행하면 다음 메뉴가 나옵니다.

```text
1. 사용자 입력 (3×3)
2. data.json 분석
3. 보너스: 2차원/1차원 성능 비교
0. 종료
```

자동 테스트는 다음 명령으로 실행합니다.

```bash
python3 -m unittest discover -v
```

## 2. 파일 구조

```text
.
├── main.py               # 메뉴와 전체 시작점
├── user_mode.py          # 3×3 사용자 입력과 재입력
├── json_mode.py          # JSON 읽기, 검증, 판정, 요약
├── mac.py                # MAC, 점수 비교, 라벨, 패턴 생성
├── performance.py        # 시간 측정, 성능표, 보너스 비교
├── data.json             # 5×5, 13×13, 25×25 데이터
├── tests/                # 31개 자동 테스트
└── README.md
```

기능별로 파일을 나눈 이유는 수정할 곳을 쉽게 찾기 위해서입니다. 계산 공식을
바꾸려면 `mac.py`, 입력 문구를 바꾸려면 `user_mode.py`, JSON 규칙을 바꾸려면
`json_mode.py`를 보면 됩니다.

## 3. 평가 기준별 구현 위치와 답변

이 절은 평가 문항을 코드와 바로 대조하기 위한 안내표입니다. 각 행의 함수에서
실제 구현을 확인할 수 있고, 자세한 원리는 링크된 README 절에서 한 번만
설명합니다.

### 항목 1: 기능 동작 검증

| 평가에서 확인하는 내용 | 구현 위치와 방식 | 검증 근거 |
| --- | --- | --- |
| 모드 1의 3×3 필터 A·B와 패턴 입력, 점수·판정·시간 출력 | `user_mode.py`의 `run_user_mode()`가 세 행렬을 입력받고 `calculate_mac()`과 `average_mac_time()`을 호출합니다. | `tests/test_user_mode.py`의 `test_user_mode_prints_scores_and_winner` |
| 허용오차 안의 동점을 `판정 불가`로 출력 | `mac.py`의 `choose_winner()`가 점수 차이를 검사하고, `user_mode.py`가 `UNDECIDED`를 쉬운 한국어로 바꿔 출력합니다. | `tests/test_mac.py`의 `test_tiny_difference_is_undecided` |
| 행·열 개수 또는 숫자 입력 오류 후 재입력 | `user_mode.py`의 `read_matrix()`가 `while` 반복문으로 틀린 행만 다시 받습니다. | `test_wrong_column_count_is_asked_again`, `test_non_number_is_asked_again` |
| `data.json` 로드 후 패턴 키의 N에 맞는 필터 선택 | `json_mode.py`의 `load_json()` → `get_size_from_key()` → `get_filters()` 순서로 처리합니다. | `tests/test_json_mode.py`의 로드·키 추출 테스트 |
| `+`·`cross`·`x`를 `Cross`·`X`로 정규화 | `mac.py`의 `normalize_label()`을 expected와 필터 키에 공통으로 사용합니다. | `tests/test_mac.py`의 세 라벨 정규화 테스트 |
| 모드 2 동점을 `UNDECIDED`와 FAIL로 집계 | `analyze_one_case()`에서 표준 판정과 expected를 비교합니다. `UNDECIDED`는 둘과 다르므로 FAIL입니다. | 현재 데이터의 `size_5_1`, `size_13_2`, `size_25_1` |
| 크기·평균 ms·N² 성능표 출력 | `performance.py`의 `measure_sizes()`가 3·5·13·25를 각각 10회 측정하고 `print_performance_table()`이 출력합니다. | `tests/test_performance.py`의 크기·N² 테스트 |
| 총 테스트·통과·실패·실패 이유 출력 | `json_mode.py`의 `count_results()`와 `print_summary()`가 집계하고 실패 목록을 출력합니다. | 현재 결과는 총 6개, 통과 3개, 실패 3개 |
| README 결과 리포트 10줄 이상 | [9. 결과 리포트](#result-report)에 실패 원인과 복잡도 분석을 14개 항목으로 정리했습니다. | 데이터·로직·수치 문제를 구분해 설명 |

### 항목 2: 코드 구조 및 설계

| 평가 질문 | 바로 답변 | 구현 위치 |
| --- | --- | --- |
| MAC의 전체 흐름은 무엇인가요? | 입력 크기를 확인한 뒤 같은 위치를 곱하고, `total`에 누적한 후 점수를 반환합니다. | `mac.py`의 `check_matrix()`와 `calculate_mac()` |
| 패턴 키와 필터는 어떻게 연결하나요? | `size_13_2`를 `_`로 나눠 N=13을 얻고 `filters["size_13"]`을 선택합니다. | `json_mode.py`의 `get_size_from_key()`와 `get_filters()` |
| 라벨 정규화를 왜 별도 함수로 뒀나요? | 문자열 규칙과 점수 계산을 분리하면 라벨 변경을 한 곳에서 처리하고 독립적으로 테스트할 수 있습니다. | `mac.py`의 `normalize_label()` |
| I/O를 제외하고 어떻게 연산 구간만 재나요? | 패턴 생성·파일 읽기·출력은 타이머 밖에 두고, `perf_counter()` 사이에는 MAC 호출 반복만 둡니다. | `performance.py`의 `average_mac_time()` |

### 항목 3: 핵심 기술 원리 적용

| 평가 질문 | 바로 답변 | 자세한 설명 |
| --- | --- | --- |
| MAC 점수가 높으면 왜 더 유사한가요? | 같은 범위의 0/1 데이터에서는 입력과 필터의 `1`이 같은 위치에 많을수록 `1×1`이 많이 더해지기 때문입니다. | [5. MAC 연산 원리](#mac-principle) |
| 왜 O(N²)인가요? | N개의 행마다 N개의 열을 방문하여 정확히 N²개 위치를 계산하기 때문입니다. | [8. 성능과 O(N²)](#performance-analysis) |
| 부동소수점 오차와 epsilon은 왜 필요한가요? | 표현 오차로 생긴 매우 작은 차이를 실제 승패로 잘못 판단하지 않기 위해 `abs(A-B) < 1e-9`를 동점으로 봅니다. | [7. 부동소수점과 epsilon](#epsilon-policy) |
| 모드 1과 모드 2의 동점 출력은 왜 다른가요? | 내부 판정은 둘 다 `UNDECIDED`입니다. 모드 1은 사용자 안내라 `판정 불가`로 번역하고, 모드 2는 expected 비교가 필요해 표준값을 유지합니다. | `user_mode.py`와 `json_mode.py`의 출력 책임 차이 |

### 항목 4: 심층 인터뷰

**새 라벨 `O`를 추가한다면 어떻게 확장하나요?**

1. `normalize_label()`에 O의 외부 표현과 표준 이름을 추가합니다.
2. `data.json`의 각 `size_N`에 O 필터를 추가합니다.
3. 두 점수만 비교하던 판정을 여러 후보 중 최대 점수를 찾도록 바꿉니다.
4. 사용자 화면과 JSON 결과에 O가 출력되도록 수정합니다.
5. 정규화·판정·동점·잘못된 데이터 테스트를 추가합니다.

정리하면 **정규화 → 판정 → 출력 → 테스트** 순서입니다.

**1000×1000 패턴에서는 어떤 문제가 생기고 무엇부터 최적화하나요?**

한 필터마다 100만 번의 곱셈이 필요하고, Python의 2차원 리스트는 각 값과 행을
관리하는 메모리도 사용합니다. 먼저 보너스에서 구현한 1차원 연속 접근으로
인덱싱을 단순화하고, 불필요한 복사를 줄인 뒤 블록 단위 처리를 검토합니다.
과제 제약 밖의 실제 제품이라면 NumPy 같은 벡터 연산이나 NPU를 사용할 수
있지만, 이 과제에서는 반복문으로 원리를 직접 확인하는 것이 우선입니다.

**CPU와 NPU에서는 병목이 어디에 생기나요?**

CPU의 Python 반복문은 N²개의 단순 연산을 대부분 순서대로 처리하므로 계산
반복 자체가 병목이 됩니다. NPU는 많은 MAC을 병렬 처리하지만, 입력을 NPU
메모리로 옮기는 시간과 메모리 대역폭이 새 병목이 될 수 있습니다. 따라서 큰
데이터를 여러 번 옮기지 않고 한 번 전송한 뒤 묶어서 계산하는 방법이 중요합니다.

### 항목 5: 보너스 구현

| 보너스 | 구현 위치 | 확인 방법 |
| --- | --- | --- |
| N×N Cross/X 자동 생성 | `mac.py`의 `make_pattern()` | 메뉴 3과 패턴 생성 테스트에서 재사용 |
| 2차원 배열을 1차원으로 변환하고 전후 성능 비교 | `flatten_matrix()`, `calculate_mac_1d()`, `compare_2d_and_1d()` | 메뉴 3에서 동일 입력·동일 10회 조건과 점수 일치를 출력 |

## 4. 프로그램 흐름

### 모드 1: 사용자 입력

```text
필터 A 3줄 입력
→ 필터 B 3줄 입력
→ 패턴 3줄 입력
→ A와 B의 MAC 점수 계산
→ epsilon으로 점수 비교
→ 점수, 시간(ms), A/B/판정 불가 출력
```

입력 예시는 다음과 같습니다.

```text
필터 A
0 1 0
1 1 1
0 1 0

필터 B
1 0 1
0 1 0
1 0 1

입력 패턴
1 0 1
0 1 0
1 0 1
```

결과 예시는 다음과 같습니다. 시간은 컴퓨터 상태에 따라 달라집니다.

```text
A 점수: 1
B 점수: 5
평균 연산 시간(10회): 0.005385 ms
판정: B
```

한 줄의 숫자가 3개가 아니거나 숫자로 바꿀 수 없는 값이면 같은 행을 다시
입력받습니다. 입력을 그만두려면 `Ctrl+C`를 누르면 됩니다.

### 모드 2: data.json 분석

```text
data.json 읽기
→ 패턴 키에서 N 추출
→ filters의 size_N 선택
→ 행렬 크기와 라벨 확인
→ Cross와 X의 MAC 점수 계산
→ expected와 비교해 PASS/FAIL 출력
→ 크기별 성능표와 전체 요약 출력
```

한 케이스의 데이터가 잘못되어도 프로그램 전체가 멈추지 않습니다. 해당
케이스만 `ERROR/FAIL`로 저장하고 다음 패턴을 계속 분석합니다.

<a id="mac-principle"></a>

## 5. MAC 연산 원리

MAC은 **Multiply-Accumulate**, 즉 “곱하고 누적해서 더하기”입니다.

입력 패턴과 필터의 같은 위치에 있는 값을 곱한 후 모든 결과를 더합니다.

```text
입력 패턴        Cross 필터
0 1 0            0 1 0
1 1 1      ×     1 1 1
0 1 0            0 1 0

점수 = 0 + 1 + 0 + 1 + 1 + 1 + 0 + 1 + 0 = 5
```

`mac.py`의 핵심은 두 겹의 반복문입니다.

```python
total = 0.0

for row in range(pattern_size):
    for column in range(pattern_size):
        multiplied_value = (
            pattern[row][column] * filter_matrix[row][column]
        )
        total = total + multiplied_value
```

현재 데이터처럼 0과 1의 범위가 같은 두 필터를 비교할 때, `1×1`이 많이
겹칠수록 점수가 커집니다. 그래서 더 높은 점수의 필터와 더 비슷하다고
판단할 수 있습니다.

AI의 신경망도 입력값과 가중치를 곱하고 더하는 일을 매우 많이 반복합니다.
따라서 MAC은 이미지 인식과 행렬 계산의 기초이며, NPU는 많은 MAC을 병렬로
빠르게 처리하도록 만든 장치입니다.

## 6. data.json 규칙

### 키 규칙

패턴 키는 `size_{N}_{idx}` 형식입니다.

```text
size_13_2
     │  └─ 두 번째 패턴
     └──── 13×13 크기
```

`json_mode.py`의 `get_size_from_key()`는 `_`를 기준으로 키를 나누고 N을
꺼냅니다. `size_13_2`라면 `filters`의 `size_13`을 선택합니다.

### 라벨 규칙

JSON과 프로그램이 같은 뜻을 다른 글자로 표현할 수 있습니다.

| JSON 표현 | 프로그램 표준 라벨 |
| --- | --- |
| `+`, `cross`, `Cross` | `Cross` |
| `x`, `X` | `X` |

`mac.py`의 `normalize_label()`이 이 값을 통일합니다. 정규화를 하지 않으면
`+`와 `Cross`가 같은 뜻인데도 문자열이 다르다는 이유로 FAIL이 생길 수
있습니다. 지원하지 않는 라벨은 마음대로 추측하지 않고 오류로 처리합니다.

<a id="epsilon-policy"></a>

## 7. 부동소수점과 epsilon

컴퓨터는 일부 실수를 정확하게 저장하지 못합니다. 예를 들어 수학적으로 거의
같은 값이 아래처럼 저장될 수 있습니다.

```text
0.9000000000000000
0.8999999999999999
```

단순히 `>`로 비교하면 첫 번째 값이 이기지만, 차이는 약 `1.11e-16`으로 매우
작습니다. 그래서 다음 정책을 사용합니다.

```text
abs(A 점수 - B 점수) < 1e-9
→ 사실상 동점
```

- 사용자 입력 모드에서는 `판정 불가`로 보여 줍니다.
- JSON 모드에서는 내부 판정값 `UNDECIDED`로 보여 줍니다.
- expected는 Cross 또는 X이므로 UNDECIDED는 FAIL로 집계됩니다.

두 모드의 계산 정책은 같고, 사용하는 사람에게 보여 주는 문구만 다릅니다.

<a id="performance-analysis"></a>

## 8. 성능과 O(N²)

`performance.py`는 파일 읽기와 화면 출력을 타이머 밖에 둡니다. 타이머 안에는
MAC 함수 호출만 넣고, 각 크기를 10회 반복한 뒤 평균 시간을 ms로 출력합니다.

| 크기 | 곱셈 위치 수 |
| --- | ---: |
| 3×3 | 9 |
| 5×5 | 25 |
| 13×13 | 169 |
| 25×25 | 625 |

N개의 행과 N개의 열을 모두 방문하므로 연산 횟수는 `N × N = N²`입니다.
따라서 시간 복잡도는 **O(N²)**입니다. 실제 측정 시간은 운영체제 부하와
Python 실행 환경의 영향을 받으므로 N² 비율과 정확히 같지는 않습니다.

<a id="result-report"></a>

## 9. 결과 리포트

현재 첨부된 `data.json`의 결과는 **총 6개, PASS 3개, FAIL 3개**입니다.

```text
총 테스트: 6개
통과: 3개
실패: 3개

실패 케이스:
- size_5_1: epsilon 범위 안의 동점
- size_13_2: epsilon 범위 안의 동점
- size_25_1: epsilon 범위 안의 동점
```

실패 원인과 복잡도 분석:

1. `size_5_2`, `size_13_1`, `size_25_2`는 판정과 expected가 같아 PASS입니다.
2. `size_5_1`의 두 점수 차이는 약 `1.11e-16`입니다.
3. 이 차이는 epsilon `1e-9`보다 작아 `UNDECIDED`입니다.
4. `size_13_2`도 점수 차이가 약 `2.66e-15`인 사실상 동점입니다.
5. `size_25_1`도 점수 차이가 약 `1.78e-15`인 사실상 동점입니다.
6. expected는 Cross 또는 X이므로 세 UNDECIDED는 FAIL로 집계됩니다.
7. 이 세 FAIL은 JSON 문법이나 행렬 크기가 잘못된 데이터 오류가 아닙니다.
8. 이 세 FAIL은 작은 실수 차이를 승패로 만들지 않는 수치 비교 정책의 결과입니다.
9. 키·라벨·크기가 잘못되면 `데이터/스키마 오류`로 따로 기록합니다.
10. 정상 계산 후 expected와 다르면 `로직/예상 불일치`로 구분할 수 있습니다.
11. MAC은 N×N의 모든 위치를 한 번씩 방문하므로 연산 횟수는 N²입니다.
12. 두 겹의 반복문 때문에 시간 복잡도는 O(N²)입니다.
13. 핵심 계산은 누적값과 반복문 위치만 사용하므로 추가 작업 공간은 O(1)입니다.
14. 측정값이 이론 비율과 정확히 같지 않은 이유는 시스템 부하와 상수 시간 때문입니다.

## 10. 실패 문제를 찾는 순서

실패가 나오면 다음 순서로 확인합니다.

1. **데이터/스키마 문제:** 키가 `size_{N}_{idx}`인지, 필터와 패턴 크기가
   같은지, Cross와 X 필터가 모두 있는지 확인합니다.
2. **로직 문제:** 같은 위치끼리 곱했는지, 두 점수를 올바른 라벨 이름으로
   비교했는지 확인합니다.
3. **수치 비교 문제:** 두 점수 차이가 epsilon보다 작은지 확인합니다.

이 순서로 보면 데이터 문제와 계산 문제를 섞지 않고 설명할 수 있습니다.

## 11. 보너스 과제

보너스 두 가지를 모두 구현했습니다.

### 패턴 자동 생성

`make_pattern(size, label)`은 원하는 크기의 Cross 또는 X를 만듭니다. 성능
분석에서도 이 함수를 재사용하므로 크기별 테스트 데이터를 직접 반복해서
작성할 필요가 없습니다.

### 2차원과 1차원 MAC 비교

`flatten_matrix()`가 N×N 행렬을 길이 N²의 1차원 리스트로 바꿉니다.
`calculate_mac_1d()`는 한 겹의 반복문으로 같은 계산을 합니다.

메뉴 3번은 같은 25×25 입력과 같은 10회 반복 조건에서 두 방법을 비교합니다.
두 점수가 같아야 변환 전후의 계산이 올바른 것입니다. 시간은 실행할 때마다
달라지므로 어느 방식이 항상 더 빠르다고 단정하지 않고 실제 측정값을 봅니다.

## 12. Git 브랜치 작업

전면 재작성은 `codex/elementary-rewrite` 브랜치에서 진행한 뒤 `main`에
`--no-ff` 방식으로 병합합니다. 이 방식은 기능 브랜치에서 작업했다는 기록을
병합 커밋으로 남깁니다.
